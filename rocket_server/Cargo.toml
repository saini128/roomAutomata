[package]
name = "rocket_server"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = { version = "0.5.1", features = ["secrets", "tls", "json"] }
rocket_cors = "0.6.0"

serde = {version= "1.0.226", features=["derive"]}
serde_json = "1.0.145"

dotenvy = "0.15.7"